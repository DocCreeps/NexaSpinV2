diff --git a/app/Livewire/Tournament/Bracket/DoubleEliminationBracketPage.php b/app/Livewire/Tournament/Bracket/DoubleEliminationBracketPage.php
index 0e7c128..49d3b6f 100644
--- a/app/Livewire/Tournament/Bracket/DoubleEliminationBracketPage.php
+++ b/app/Livewire/Tournament/Bracket/DoubleEliminationBracketPage.php
@@ -62,6 +62,20 @@ public function mount(): void
         $this->results = $saved['results'] ?? [];
         $this->started = $saved['started'] ?? false;
 
+        // Pré-remplit les champs de score depuis les résultats déjà enregistrés :
+        // le mode "édition" (voir la vue) peut ainsi afficher les valeurs
+        // existantes sans jamais avoir à toucher $this->results juste pour les
+        // consulter — cliquer sur ✎ puis annuler ne doit rien modifier.
+        foreach ($this->results as $r) {
+            $prefix = $r['round'] !== null ? "{$r['section']}_{$r['round']}_{$r['position']}" : $r['section'];
+            if (array_key_exists('score_a', $r)) {
+                $this->scores["{$prefix}_a"] = $r['score_a'];
+            }
+            if (array_key_exists('score_b', $r)) {
+                $this->scores["{$prefix}_b"] = $r['score_b'];
+            }
+        }
+
         if ($this->started && $this->participants !== []) {
             $bracket = app(RebuildDoubleEliminationBracketAction::class)->execute($this->participants, $this->results);
             $this->champion = $bracket->isComplete() ? $bracket->champion()?->name : null;
@@ -169,6 +183,7 @@ public function recordResult(string $section, ?int $round, ?int $position, ?stri
         }
 
         $this->error = null;
+        $wasComplete = $this->champion !== null;
 
         $match = $this->findMatch($section, $round, $position);
 
@@ -209,10 +224,34 @@ public function recordResult(string $section, ?int $round, ?int $position, ?stri
             return;
         }
 
+        // S'il existe déjà un résultat pour ce match (correction), on repart
+        // des résultats *sans* son ancienne entrée. hasDownstreamResult()
+        // garantit qu'aucun match suivant n'a encore exploité l'ancien
+        // résultat — sinon on refuse plutôt que de laisser un état incohérent.
+        $existingIndex = null;
+        foreach ($this->results as $i => $r) {
+            if ($r['section'] === $section && $r['round'] === $round && $r['position'] === $position) {
+                $existingIndex = $i;
+                break;
+            }
+        }
+
+        if ($existingIndex !== null && $this->bracket()->hasDownstreamResult($section, $round, $position)) {
+            $this->error = 'Ce match a déjà influencé un match suivant (vainqueur propagé ou perdant repêché) : impossible de le modifier tant que ce match suivant n’est pas lui-même annulé.';
+            return;
+        }
+
+        $previousResults = $existingIndex === null
+            ? $this->results
+            : array_values(array_filter(
+                $this->results,
+                fn (array $r) => ! ($r['section'] === $section && $r['round'] === $round && $r['position'] === $position)
+            ));
+
         try {
             $bracket = app(RecordDoubleEliminationMatchResultAction::class)->execute(
                 $this->participants,
-                $this->results,
+                $previousResults,
                 $section,
                 $round,
                 $position,
@@ -223,7 +262,7 @@ public function recordResult(string $section, ?int $round, ?int $position, ?stri
             return;
         }
 
-        $this->results[] = [
+        $previousResults[] = [
             'section' => $section,
             'round' => $round,
             'position' => $position,
@@ -231,12 +270,17 @@ public function recordResult(string $section, ?int $round, ?int $position, ?stri
             'score_a' => is_numeric($valA) ? (int) $valA : null,
             'score_b' => is_numeric($valB) ? (int) $valB : null,
         ];
+        $this->results = $previousResults;
 
         unset($this->bracket);
 
-        if ($bracket->isComplete()) {
-            $this->champion = $bracket->champion()?->name;
+        // Toujours recalculé (pas seulement si complet) : une correction peut
+        // aussi bien faire apparaître un besoin de reset que le faire
+        // disparaître, et saveProgress() se fie à $this->champion pour savoir
+        // s'il doit vider ou conserver la progression sauvegardée.
+        $this->champion = $bracket->isComplete() ? $bracket->champion()?->name : null;
 
+        if ($this->champion !== null && ! $wasComplete) {
             app(HistoryStore::class)->push(self::MODE, [
                 'champion' => $this->champion,
                 'participants' => $this->participants,
@@ -263,56 +307,6 @@ public function autoRecordIfReady(string $section, ?int $round, ?int $position):
         }
     }
 
-    /**
-     * Rouvre un match déjà résolu pour correction. Un match de bracket peut
-     * avoir des matchs suivants qui dépendent directement de son vainqueur
-     * (propagation upper/lower) ; hasDownstreamResult() vérifie que ce n'est
-     * pas encore le cas avant d'autoriser quoi que ce soit — sinon on
-     * refuse (voir plus bas) plutôt que d'effacer les résultats suivants.
-     * Une fois cette garantie acquise, on peut donc se contenter de retirer
-     * la seule entrée de ce match, exactement comme pour les poules (cf.
-     * PoolStagePage::editResult()).
-     */
-    public function editResult(string $section, ?int $round, ?int $position): void
-    {
-        if (! $this->started) {
-            return;
-        }
-
-        $this->error = null;
-
-        $index = null;
-        foreach ($this->results as $i => $r) {
-            if ($r['section'] === $section && $r['round'] === $round && $r['position'] === $position) {
-                $index = $i;
-                break;
-            }
-        }
-
-        if ($index === null) {
-            return;
-        }
-
-        if ($this->bracket()->hasDownstreamResult($section, $round, $position)) {
-            $this->error = 'Ce match a déjà influencé un match suivant (vainqueur propagé ou perdant repêché) : impossible de le modifier tant que ce match suivant n’est pas lui-même annulé.';
-            return;
-        }
-
-        $existing = $this->results[$index];
-        unset($this->results[$index]);
-        $this->results = array_values($this->results);
-
-        $prefix = $round !== null ? "{$section}_{$round}_{$position}" : $section;
-        $this->scores["{$prefix}_a"] = $existing['score_a'] ?? null;
-        $this->scores["{$prefix}_b"] = $existing['score_b'] ?? null;
-
-        $this->champion = null;
-
-        unset($this->bracket);
-
-        $this->saveProgress();
-    }
-
     private function findMatch(string $section, ?int $round, ?int $position)
     {
         $bracket = $this->bracket();
diff --git a/app/Livewire/Tournament/Pool/PoolStagePage.php b/app/Livewire/Tournament/Pool/PoolStagePage.php
index fb3d1f6..783aaf0 100644
--- a/app/Livewire/Tournament/Pool/PoolStagePage.php
+++ b/app/Livewire/Tournament/Pool/PoolStagePage.php
@@ -65,6 +65,19 @@ public function mount(): void
         $this->started = $saved['started'] ?? false;
         $this->withScores = $saved['withScores'] ?? true;
 
+        // Pré-remplit les champs de score depuis les résultats déjà enregistrés,
+        // pour que le mode "édition" (voir la vue) puisse afficher les valeurs
+        // existantes sans jamais avoir à toucher $this->results juste pour les
+        // consulter — cliquer sur ✎ puis annuler ne doit rien modifier.
+        foreach ($this->results as $r) {
+            if (array_key_exists('score_a', $r)) {
+                $this->scores["{$r['pool']}_{$r['matchIndex']}_a"] = $r['score_a'];
+            }
+            if (array_key_exists('score_b', $r)) {
+                $this->scores["{$r['pool']}_{$r['matchIndex']}_b"] = $r['score_b'];
+            }
+        }
+
         if ($this->started && $this->participants !== []) {
             $this->isComplete = $this->stage()->isComplete();
         }
@@ -292,44 +305,6 @@ public function recordResult(string $poolName, int $matchIndex, ?string $manualW
         $this->saveProgress();
     }
 
-    /**
-     * Rouvre un match déjà résolu pour correction : retire son résultat
-     * enregistré (les matchs de poule étant indépendants les uns des autres,
-     * cela ne remet en cause aucun autre résultat) et pré-remplit les champs
-     * de score avec les valeurs précédentes pour faciliter la correction.
-     */
-    public function editResult(string $poolName, int $matchIndex): void
-    {
-        if (! $this->started) {
-            return;
-        }
-
-        $this->error = null;
-
-        $existing = collect($this->results)->firstWhere(
-            fn (array $r) => $r['pool'] === $poolName && $r['matchIndex'] === $matchIndex
-        );
-
-        if ($existing === null) {
-            return;
-        }
-
-        $this->results = array_values(array_filter(
-            $this->results,
-            fn (array $r) => ! ($r['pool'] === $poolName && $r['matchIndex'] === $matchIndex)
-        ));
-
-        $keyA = "{$poolName}_{$matchIndex}_a";
-        $keyB = "{$poolName}_{$matchIndex}_b";
-        $this->scores[$keyA] = $existing['score_a'] ?? null;
-        $this->scores[$keyB] = $existing['score_b'] ?? null;
-
-        $this->isComplete = false;
-
-        unset($this->stage);
-
-        $this->saveProgress();
-    }
 
     /**
      * Valide automatiquement un match dès que les deux scores sont saisis
diff --git a/resources/views/livewire/tournament/bracket/partials/match-card.blade.php b/resources/views/livewire/tournament/bracket/partials/match-card.blade.php
index 931a380..70d910b 100644
--- a/resources/views/livewire/tournament/bracket/partials/match-card.blade.php
+++ b/resources/views/livewire/tournament/bracket/partials/match-card.blade.php
@@ -21,7 +21,7 @@
         && ! $this->bracket->hasDownstreamResult($section, $round, $position);
 @endphp
 
-<div class="card-hard flex items-center gap-2 rounded-lg border-2 border-ink bg-panel px-2.5 py-2 text-xs shadow-hard" title="{{ $label }}">
+<div class="card-hard flex items-center gap-2 rounded-lg border-2 border-ink bg-panel px-2.5 py-2 text-xs shadow-hard" title="{{ $label }}" x-data="{ editing: false }">
     <span @class([
         'h-1.5 w-1.5 shrink-0 rounded-full',
         'bg-faint/40' => $isBye,
@@ -40,18 +40,40 @@
         <span class="text-faint">:</span>
         <input type="number" min="0" wire:model.defer="scores.{{ $prefix }}_b" wire:change="autoRecordIfReady('{{ $section }}', {{ $roundArg }}, {{ $positionArg }})" class="h-7 w-9 rounded-md border border-ink bg-wash text-center focus:outline-none focus:ring-2 focus:ring-info" placeholder="–">
         @elseif($isResolved)
-        <span class="{{ $winner && $a && $winner->equals($a) ? 'text-ink' : 'text-subtle' }}">{{ $savedResult['score_a'] ?? '–' }}</span>
-        <span class="text-faint">:</span>
-        <span class="{{ $winner && $b && $winner->equals($b) ? 'text-ink' : 'text-subtle' }}">{{ $savedResult['score_b'] ?? '–' }}</span>
-        @if($isEditable)
-        <button type="button" wire:click="editResult('{{ $section }}', {{ $roundArg }}, {{ $positionArg }})" title="Modifier le résultat" class="ml-1 rounded p-1 text-faint transition hover:bg-wash hover:text-ink">
-            ✎
-        </button>
-        @elseif($savedResult !== null)
-        <span title="Déjà répercuté sur un match suivant : impossible à modifier tant que celui-ci n'est pas annulé" class="ml-1 cursor-not-allowed rounded p-1 text-faint/40">
-            ✎
-        </span>
-        @endif
+            @if($isEditable)
+            {{-- Ni l'affichage statique ni son édition ne font d'aller-retour
+                 serveur pour basculer de l'un à l'autre : un clic accidentel
+                 sur ✎ se corrige d'un clic sur ✕, sans rien avoir modifié. --}}
+            <template x-if="! editing">
+                <div class="flex items-center gap-1">
+                    <span class="{{ $winner && $a && $winner->equals($a) ? 'text-ink' : 'text-subtle' }}">{{ $savedResult['score_a'] ?? '–' }}</span>
+                    <span class="text-faint">:</span>
+                    <span class="{{ $winner && $b && $winner->equals($b) ? 'text-ink' : 'text-subtle' }}">{{ $savedResult['score_b'] ?? '–' }}</span>
+                    <button type="button" x-on:click="editing = true" title="Modifier le résultat" class="ml-1 rounded p-1 text-faint transition hover:bg-wash hover:text-ink">
+                        ✎
+                    </button>
+                </div>
+            </template>
+            <template x-if="editing">
+                <div class="flex items-center gap-1">
+                    <input type="number" min="0" wire:model.defer="scores.{{ $prefix }}_a" wire:change="autoRecordIfReady('{{ $section }}', {{ $roundArg }}, {{ $positionArg }})" class="h-7 w-9 rounded-md border border-ink bg-wash text-center focus:outline-none focus:ring-2 focus:ring-info">
+                    <span class="text-faint">:</span>
+                    <input type="number" min="0" wire:model.defer="scores.{{ $prefix }}_b" wire:change="autoRecordIfReady('{{ $section }}', {{ $roundArg }}, {{ $positionArg }})" class="h-7 w-9 rounded-md border border-ink bg-wash text-center focus:outline-none focus:ring-2 focus:ring-info">
+                    <button type="button" x-on:click="editing = false" title="Annuler" class="ml-1 rounded p-1 text-faint transition hover:bg-wash hover:text-ink">
+                        ✕
+                    </button>
+                </div>
+            </template>
+            @else
+            <span class="{{ $winner && $a && $winner->equals($a) ? 'text-ink' : 'text-subtle' }}">{{ $savedResult['score_a'] ?? '–' }}</span>
+            <span class="text-faint">:</span>
+            <span class="{{ $winner && $b && $winner->equals($b) ? 'text-ink' : 'text-subtle' }}">{{ $savedResult['score_b'] ?? '–' }}</span>
+            @if($savedResult !== null)
+            <span title="Déjà répercuté sur un match suivant : impossible à modifier tant que celui-ci n'est pas annulé" class="ml-1 cursor-not-allowed rounded p-1 text-faint/40">
+                ✎
+            </span>
+            @endif
+            @endif
         @else
         <span class="text-faint">– : –</span>
         @endif
diff --git a/resources/views/livewire/tournament/pool/pool-stage-page.blade.php b/resources/views/livewire/tournament/pool/pool-stage-page.blade.php
index 7488b85..2f1ed74 100644
--- a/resources/views/livewire/tournament/pool/pool-stage-page.blade.php
+++ b/resources/views/livewire/tournament/pool/pool-stage-page.blade.php
@@ -182,7 +182,7 @@
 
                         @if($withScores)
                         {{-- Ligne compacte : Nom  score VS score  Nom, validation automatique --}}
-                        <div class="flex items-center gap-2 rounded-lg border-2 border-ink/10 bg-wash px-2.5 py-2 text-xs">
+                        <div class="flex items-center gap-2 rounded-lg border-2 border-ink/10 bg-wash px-2.5 py-2 text-xs" x-data="{ editing: false }">
                             <span class="min-w-0 flex-1 truncate font-bold {{ $isResolved && ! $isDraw && $winner->equals($match->participantA()) ? 'text-ink' : 'text-ink' }}">
                                 {{ $isResolved && ! $isDraw && $winner->equals($match->participantA()) ? '🏆 ' : '' }}{{ $match->participantA()->name }}
                             </span>
@@ -192,19 +192,37 @@
                                 <input type="number" min="0" wire:model.defer="scores.{{ $keyA }}" wire:change="autoRecordIfReady('{{ $pool->name }}', {{ $match->index }})" class="h-7 w-9 rounded-md border border-ink bg-panel text-center focus:outline-none focus:ring-2 focus:ring-info" placeholder="–">
                                 <span class="text-faint">:</span>
                                 <input type="number" min="0" wire:model.defer="scores.{{ $keyB }}" wire:change="autoRecordIfReady('{{ $pool->name }}', {{ $match->index }})" class="h-7 w-9 rounded-md border border-ink bg-panel text-center focus:outline-none focus:ring-2 focus:ring-info" placeholder="–">
-                                @elseif($isDraw)
-                                <span class="text-subtle">{{ $savedResult['score_a'] ?? '–' }}</span>
-                                <span class="text-faint">:</span>
-                                <span class="text-subtle">{{ $savedResult['score_b'] ?? '–' }}</span>
                                 @else
-                                <span class="{{ $winner->equals($match->participantA()) ? 'text-ink' : 'text-subtle' }}">{{ $savedResult['score_a'] ?? '–' }}</span>
-                                <span class="text-faint">:</span>
-                                <span class="{{ $winner->equals($match->participantB()) ? 'text-ink' : 'text-subtle' }}">{{ $savedResult['score_b'] ?? '–' }}</span>
-                                @endif
-                                @if($isResolved)
-                                <button type="button" wire:click="editResult('{{ $pool->name }}', {{ $match->index }})" title="Modifier le résultat" class="ml-1 rounded p-1 text-faint transition hover:bg-wash hover:text-ink">
-                                    ✎
-                                </button>
+                                {{-- Résolu : affichage statique + ✎, ou en édition + ✕. Ni l'un ni
+                                     l'autre ne fait d'aller-retour serveur tant que le score n'a
+                                     pas réellement changé — un clic accidentel sur ✎ se corrige en
+                                     un clic sur ✕, sans rien avoir modifié. --}}
+                                <template x-if="! editing">
+                                    <div class="flex items-center gap-1">
+                                        @if($isDraw)
+                                        <span class="text-subtle">{{ $savedResult['score_a'] ?? '–' }}</span>
+                                        <span class="text-faint">:</span>
+                                        <span class="text-subtle">{{ $savedResult['score_b'] ?? '–' }}</span>
+                                        @else
+                                        <span class="{{ $winner->equals($match->participantA()) ? 'text-ink' : 'text-subtle' }}">{{ $savedResult['score_a'] ?? '–' }}</span>
+                                        <span class="text-faint">:</span>
+                                        <span class="{{ $winner->equals($match->participantB()) ? 'text-ink' : 'text-subtle' }}">{{ $savedResult['score_b'] ?? '–' }}</span>
+                                        @endif
+                                        <button type="button" x-on:click="editing = true" title="Modifier le résultat" class="ml-1 rounded p-1 text-faint transition hover:bg-wash hover:text-ink">
+                                            ✎
+                                        </button>
+                                    </div>
+                                </template>
+                                <template x-if="editing">
+                                    <div class="flex items-center gap-1">
+                                        <input type="number" min="0" wire:model.defer="scores.{{ $keyA }}" wire:change="autoRecordIfReady('{{ $pool->name }}', {{ $match->index }})" class="h-7 w-9 rounded-md border border-ink bg-panel text-center focus:outline-none focus:ring-2 focus:ring-info">
+                                        <span class="text-faint">:</span>
+                                        <input type="number" min="0" wire:model.defer="scores.{{ $keyB }}" wire:change="autoRecordIfReady('{{ $pool->name }}', {{ $match->index }})" class="h-7 w-9 rounded-md border border-ink bg-panel text-center focus:outline-none focus:ring-2 focus:ring-info">
+                                        <button type="button" x-on:click="editing = false" title="Annuler" class="ml-1 rounded p-1 text-faint transition hover:bg-wash hover:text-ink">
+                                            ✕
+                                        </button>
+                                    </div>
+                                </template>
                                 @endif
                             </div>
 
@@ -214,7 +232,7 @@
                         </div>
                         @else
                         {{-- Sans scores : boutons Victoire / Nul, toujours en une ligne --}}
-                        <div class="flex items-center gap-2 rounded-lg border-2 border-ink/10 bg-wash px-2.5 py-2 text-xs">
+                        <div class="flex items-center gap-2 rounded-lg border-2 border-ink/10 bg-wash px-2.5 py-2 text-xs" x-data="{ editing: false }">
                             <span class="min-w-0 flex-1 truncate font-bold text-ink">
                                 {{ $isResolved && ! $isDraw && $winner->equals($match->participantA()) ? '🏆 ' : '' }}{{ $match->participantA()->name }}
                             </span>
@@ -231,12 +249,32 @@
                                     Victoire
                                 </button>
                                 @else
-                                @if($isDraw)
-                                <span class="rounded-md border border-ink/20 bg-panel px-1.5 py-0.5 font-mono text-[9px] uppercase tracking-widest text-subtle">Nul</span>
-                                @endif
-                                <button type="button" wire:click="editResult('{{ $pool->name }}', {{ $match->index }})" title="Modifier le résultat" class="rounded p-1 text-faint transition hover:bg-wash hover:text-ink">
-                                    ✎
-                                </button>
+                                <template x-if="! editing">
+                                    <div class="flex items-center gap-1">
+                                        @if($isDraw)
+                                        <span class="rounded-md border border-ink/20 bg-panel px-1.5 py-0.5 font-mono text-[9px] uppercase tracking-widest text-subtle">Nul</span>
+                                        @endif
+                                        <button type="button" x-on:click="editing = true" title="Modifier le résultat" class="rounded p-1 text-faint transition hover:bg-wash hover:text-ink">
+                                            ✎
+                                        </button>
+                                    </div>
+                                </template>
+                                <template x-if="editing">
+                                    <div class="flex items-center gap-1">
+                                        <button type="button" wire:click="recordResult('{{ $pool->name }}', {{ $match->index }}, '{{ addslashes($match->participantA()->name) }}')" class="btn-press rounded border border-ink bg-panel px-1.5 py-0.5 font-mono text-[9px] uppercase text-subtle hover:bg-ink hover:text-white">
+                                            Victoire
+                                        </button>
+                                        <button type="button" wire:click="recordResult('{{ $pool->name }}', {{ $match->index }}, null, true)" class="btn-press rounded border border-ink bg-panel px-1.5 py-0.5 font-mono text-[9px] uppercase text-subtle hover:bg-ink hover:text-white">
+                                            Nul
+                                        </button>
+                                        <button type="button" wire:click="recordResult('{{ $pool->name }}', {{ $match->index }}, '{{ addslashes($match->participantB()->name) }}')" class="btn-press rounded border border-ink bg-panel px-1.5 py-0.5 font-mono text-[9px] uppercase text-subtle hover:bg-ink hover:text-white">
+                                            Victoire
+                                        </button>
+                                        <button type="button" x-on:click="editing = false" title="Annuler" class="rounded p-1 text-faint transition hover:bg-wash hover:text-ink">
+                                            ✕
+                                        </button>
+                                    </div>
+                                </template>
                                 @endif
                             </div>
 
